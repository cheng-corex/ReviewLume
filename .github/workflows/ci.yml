name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node-version: 20
          - os: ubuntu-latest
            node-version: 22
          - os: windows-latest
            node-version: 22
          - os: macos-latest
            node-version: 22

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        shell: bash
        run: pnpm typecheck 2>&1 | tee typecheck-output.txt

      - name: Upload typecheck output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-typecheck-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: typecheck-output.txt
          if-no-files-found: error

      - name: Test
        shell: bash
        run: pnpm test 2>&1 | tee test-output.txt

      - name: Upload test output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-test-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: test-output.txt
          if-no-files-found: error

      - name: Build
        run: pnpm build

      - name: Package VSIX
        run: pnpm package:vscode

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-vscode-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: reviewlume-vscode-*.vsix
          if-no-files-found: error

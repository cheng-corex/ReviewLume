name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node-version: 20
          - os: ubuntu-latest
            node-version: 22
          - os: windows-latest
            node-version: 22
          - os: macos-latest
            node-version: 22

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck > typecheck-output-${{ matrix.os }}.txt 2>&1

      - name: Upload failing typecheck output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-typecheck-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: typecheck-output-${{ matrix.os }}.txt
          if-no-files-found: error

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Package VSIX
        run: pnpm package:vscode > package-output-${{ matrix.os }}.txt 2>&1

      - name: Upload failing package output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-package-output-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: package-output-${{ matrix.os }}.txt
          if-no-files-found: error

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: reviewlume-vscode-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: reviewlume-vscode-*.vsix
          if-no-files-found: error

# Stable MCP Tool Contract

ReviewLume advertises the same 11 read-only MCP tool names and public input schemas for both Git Project and Folder Project connections.

This avoids stale ChatGPT tool snapshots when the same Tunnel switches project kind. Folder mode still does not run Local Verification; its verification evidence tool calls return unavailable without starting a process.
